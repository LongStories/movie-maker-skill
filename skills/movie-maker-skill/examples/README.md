# Examples

This folder contains **static examples** (no executable pipeline) showing the intended artifacts:

- A numbered script (`script.txt`)
- A scene+shot-based shotlist referencing script lines (`shotlist.txt`)
- A manifest JSON combining both (`manifest.json`)

Start with:
- `examples/shipped/`
