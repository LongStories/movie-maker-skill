# movie-maker-skill (docs-first)
# This file is a template for your *local* pipeline repo.
# This repository does not include an executable pipeline.

# Required if you generate voiceover with ElevenLabs:
ELEVENLABS_API_KEY=

# Required if you generate images/videos with fal:
FAL_API_KEY=

# Rendering (ffmpeg) has no env vars; it must be installed on PATH.
# Upload credentials live in the `clawtube` repo.
